# Handle the fact that this file will be used with multiple OSs
platform=`uname`
if [[ $platform == 'Linux' ]]; then
  alias a='ls -lrth --color'
elif [[ $platform == 'Darwin' ]]; then
  alias a='ls -lrthG'
fi

alias aliases='mate ~/.dotfiles/bash/aliases'
alias d='cd ~/dotfiles'
alias so='source ~/dotfiles/bash/aliases'
alias di='d; rake install'

alias s='cd ~/code/slidelongboards'
alias t='cd ~/code/benefic'
alias k='cd ~/code/kya'
alias t='cd ~/code/clac-training'
alias j='cd ~/code/clac-jobs'

alias g='git status'
alias gc='git commit'
alias gca='git commit -a'
alias gcm='git commit -m'
alias gco='git checkout'
alias gd='git diff'
alias gdc='git diff --cached'
alias get='sudo apt-get install'
alias gg='git log'

alias irb='irb --readline -r irb/completion'

alias rdm="rake db:migrate"
alias sc='script/console'
alias ss='launchy http://localhost:3000; ruby script/server -p 3000;'

